# Backend API base URL — used for server-side fetches AND rewrites.
# Leave unset to default to http://localhost:3000 (the Express server).
API_BASE_URL=http://localhost:3000

# Public site URL (used in <meta> for SEO)
NEXT_PUBLIC_SITE_URL=http://localhost:3001
